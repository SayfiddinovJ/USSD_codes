import 'package:flutter/material.dart';

class UcellTabsBox extends StatelessWidget {
  const UcellTabsBox({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Ucell'),
        backgroundColor: Colors.deepPurple,
      ),
    );
  }
}
