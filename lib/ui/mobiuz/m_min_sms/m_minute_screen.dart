import 'package:flutter/material.dart';

class MMinuteScreen extends StatelessWidget {
  const MMinuteScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Daqiqalar'),
        backgroundColor: Colors.red,
      ),
    );
  }
}
