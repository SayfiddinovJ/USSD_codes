import 'package:flutter/material.dart';

class MMinSMSScreen extends StatelessWidget {
  const MMinSMSScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Daq/SMS"),
        backgroundColor: Colors.red,
      ),
    );
  }
}
