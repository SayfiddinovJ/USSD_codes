import 'package:flutter/material.dart';

class MOperatorScreen extends StatelessWidget {
  const MOperatorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Operatorlar"),
        backgroundColor: Colors.red,
      ),
    );
  }
}
